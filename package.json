{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "nuxt test"
  },
  "devDependencies": {
    "@nuxt/test-utils": "^3.2.2",
    "nuxt": "^3.2.2",
    "vitest": "^0.28.5"
  }
}
